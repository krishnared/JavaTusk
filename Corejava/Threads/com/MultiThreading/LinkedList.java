package com.MultiThreading;

public class LinkedList {
//Linked List follows the concept of the double linked list 
	//A chain is formed when the element is inserted
	//Each and every value has the previous and the next node connection
	//Using that the value can be inserted in between
	//If inserting a value in between the values -> Then DLL is prefered 
	//How do we search in Linked list ?
}
